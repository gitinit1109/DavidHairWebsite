# Environment Variables

# AI 客服 (server.ts /api/chat) — used ONLY server-side, never exposed to the
# browser, so it stays out of the client bundle. Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

VITE_YOUTUBE_API_KEY=
